#include <stdio.h>
#include <stdlib.h>
#include "TAD_linkedList.h"

int main(){

    ITEM* lista;

    lista = createItem(lista);

    insertElement(lista);

    insertElement(lista);

    printList(lista);

    searchItem(lista);

    printList(lista);

    return 0;
}
